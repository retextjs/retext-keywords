declare module 'nlcst' {
  // eslint-disable-next-line @typescript-eslint/consistent-type-definitions
  interface WordData {
    /**
     * Registered by `types.js` from `retext-keywords` (website branch).
     */
    score?: number | undefined
  }
}

// Turn into a module.
export {}
